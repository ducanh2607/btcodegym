package stackqueue;
import java.util.*;

public class CountChar {
    static void characterCount(String inputString){
        String str = "Hello";
        Map<String, Integer> map = new TreeMap<>();
        String str1 = "";
        for (int i = 0; i < str.length(); i++) {
            str1 += Character.isUpperCase(str.charAt(i));
        }
//        for (int i = 0; i < str1.length(); i++) {
//            map.
//        }


    }
}
